<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title>Test Page</title>
</head>
<body>
  <h1>Test Page - {{ products.size }} products</h1>
  <p>Settings: {{ settings | json }}</p>
  <p>Collections: {{ collections.size }}</p>
  
  <div class="topbar">
    <div class="topbar__marquee">
      <div class="topbar__track">
        <span class="topbar__item">FREE SHIPPING FOR ORDERS OVER $100</span>
        <span class="topbar__item">FREE SHIPPING FOR ORDERS OVER $100</span>
      </div>
    </div>
  </div>
  
  <header class="header">
    <div class="header__inner">
      <div class="header__logo">
        <a href="/">SHUFFLE</a>
      </div>
      <nav class="header__nav">
        <ul class="header__menu">
          <li><a href="/">Home</a></li>
          <li><a href="/products">Products</a></li>
        </ul>
      </nav>
    </div>
  </header>
  
  <section class="hero-banner" style="background-image: url('/assets/hero_banner_img.avif');">
    <div class="hero-banner__content">
      <a href="/collections/all" class="hero-banner__button">Shop Now</a>
    </div>
  </section>
  
  <section class="featured-products">
    <h2>Our Featured Products</h2>
    <div class="product-list">
      {% for product in products limit: 4 %}
        <div class="product-card">
          <div class="product-card__image" style="background-image: url('/assets/{{ product.image }}');"></div>
          <div class="product-card__content">
            <h3>{{ product.title }}</h3>
            <div class="product-card__price">${{ product.price }}</div>
            <p class="product-card__description">{{ product.description }}</p>
          </div>
        </div>
      {% endfor %}
    </div>
  </section>
  
  <footer class="footer">
    <div class="footer__container">
      <div class="footer__brand">
        <h3 class="footer__logo">SHUFFLE</h3>
        <p class="footer__description">Your premium destination for quality products.</p>
      </div>
    </div>
  </footer>
</body>
</html>
